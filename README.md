# CMU-16-745

Start VS Code, run the Dev Containers: Open Folder in Container... command from the Command Palette (F1) or quick actions Status bar item.

To build the doco do

```bash
jupyter-book build optimalcontrol
```